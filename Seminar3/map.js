const array = [1, 2, 3, 4];

const forEachArray = array.forEach(item => {
    // console.log(item);
    return item;
})

const mapped = array.map(item => {
    // console.log(item);
    return item;
})

console.log(forEachArray);
console.log(mapped);